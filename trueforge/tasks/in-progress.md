# In Progress

## Task 001 — Foundation
- **Owner**: Architect (orchestrator, this pass) → Backend + Frontend (parallel child agents)
- **Started**: TrueForge bootstrap.
- **Current step**: Architect phase (TrueForge docs, root workspace config, schema/API contract)
  in progress. Backend and Frontend child agents to implement `apps/api` and `apps/web` next.
- **Files expected to change**: `apps/api/**`, `apps/web/**`, `prisma/**`.
- **Blockers**: none (repo now has a persistent remote at
  https://github.com/ZeroCoolrules/digiCasino.git).
