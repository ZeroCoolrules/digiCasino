# Scripts

No executable scripts exist yet. Per the TrueForge v1 scope, markdown-based agent roles and a
structured task registry are sufficient for now — do not add executable orchestration
infrastructure here until a demonstrated need arises (e.g. a script to auto-move tasks between
lifecycle files, once the manual workflow has been used enough to know what to automate).
